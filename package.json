{
  "name": "studypath-ai-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
